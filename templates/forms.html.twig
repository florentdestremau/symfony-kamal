{% block textarea_widget %}
    <lexxy-editor name="post[body]" attachments="false" value="{{ form.vars.value|raw }}"
                  placeholder="{{ form.vars.placeholder ?? '' }}">
    </lexxy-editor>
{% endblock %}
